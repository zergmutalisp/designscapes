{
  "name": "designscapes",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "test": "node --test demos/mortgage-paydown/mortgage-core.test.mjs",
    "test:e2e": "playwright test",
    "build:napoleon": "npm --prefix sources/napoleon run build -- --outDir ../../demos/napoleon --emptyOutDir"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
